server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cardword?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

# 文件上传配置
upload:
  path: "C:\\Users\\likexin\\Pictures\\image"
  max-size: 10MB
  allowed-types: image/jpeg,image/png,image/webp
